# РемонтCRM

Веб-приложение для управления заявками, рекламой и бухгалтерией службы бытового ремонта.
Стек: Next.js 16 (App Router) + TypeScript + Prisma + NextAuth v5 (Auth.js) + Tailwind CSS + PostgreSQL.

## Локальный запуск

```bash
npm install
npm run db:setup       # prisma generate + db push (создаёт таблицы) + seed (демо-данные)
npm run dev
```

Приложение поднимется на http://localhost:3000.

Перед этим создайте файл `.env` на основе `.env.example` и укажите строку подключения
к PostgreSQL (см. ниже — проще всего взять бесплатную базу на neon.com) и `AUTH_SECRET`.

### Демо-доступы (после `db:seed`, пароль везде `password123`)

| Логин        | Роль                    |
|--------------|-------------------------|
| director     | Директор                |
| regional     | Региональный директор   |
| admin        | Администратор           |
| callcenter   | Колл-центр              |
| master1      | Мастер                  |
| master2      | Мастер                  |

## База данных — PostgreSQL

Проект использует PostgreSQL и локально, и в проде (SQLite не подходит для деплоя на
serverless-хостинги вроде Vercel — там файловая система эфемерная и не расшаривается
между вызовами функций, поэтому файл базы просто не сохранится между запросами).

Самый быстрый способ получить бесплатную PostgreSQL-базу:

1. Зайдите на **https://neon.com**, зарегистрируйтесь (можно через GitHub)
2. Создайте новый проект/базу — Neon сразу покажет **Connection string** вида
   `postgresql://user:password@ep-xxx.neon.tech/dbname?sslmode=require`
3. Вставьте эту строку в `.env` (локально) и в переменные окружения хостинга (для прода)

Альтернативы: Supabase, Vercel Postgres, Railway — подход тот же, нужна одна строка подключения.

## Деплой — как получить ссылку на сайт

GitHub-репозиторий и релиз сами по себе ничего не разворачивают — это просто хранилище
кода. Чтобы получить рабочую ссылку на сайт, нужно подключить репозиторий к хостингу.
Проще всего — **Vercel** (создатели Next.js, бесплатный тариф достаточен для этого проекта):

1. Зайдите на **https://vercel.com**, зарегистрируйтесь через GitHub
2. **Add New → Project** → выберите ваш репозиторий `remont-crm`
3. Framework Preset определится автоматически как Next.js — ничего менять не нужно
4. Перед деплоем откройте **Environment Variables** и добавьте:
   - `DATABASE_URL` — строка подключения к Neon (см. выше)
   - `AUTH_SECRET` — случайная строка (сгенерировать: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`)
   - `NEXTAUTH_URL` — на первый деплой можно не указывать, Vercel подставит домен сам;
     после первого деплоя скопируйте выданный домен (например `https://remont-crm.vercel.app`)
     и добавьте его сюда явно, затем передеплойте
5. Нажмите **Deploy**

После деплоя Vercel выдаст ссылку вида `https://remont-crm-<случайные-символы>.vercel.app`
(или `https://remont-crm.vercel.app`, если имя свободно) — это и есть ссылка на сайт.
Она также всегда видна в панели Vercel на странице проекта.

### Создать таблицы в продакшен-базе

Схема (модели, поля) сама по себе не создаёт таблицы в новой базе — это нужно сделать
один раз командой `prisma db push`. Проще всего выполнить её **локально**, указав в
`.env` строку подключения к вашей продакшен-базе (той же, что в Vercel):

```bash
npm run db:push
npm run db:seed    # необязательно — создаст демо-пользователей и тестовые заявки
```

После этого сайт на Vercel сразу увидит созданные таблицы и данные — база одна и та же.

## Структура проекта

```
prisma/schema.prisma        — модель данных (раздел 9 ТЗ)
prisma/seed.ts               — демо-данные
lib/domain.ts                 — роли, статусы, права разделов — единый источник правды
lib/rbac.ts                    — серверная проверка прав (403), временные окна редактирования
lib/money.ts                    — деление суммы закрытия (раздел 2.4)
lib/auth.ts                      — конфигурация NextAuth v5
lib/notify.ts                     — уведомление мастера (in-app сразу, email/push — заглушки)
middleware.ts                      — редирект неавторизованных, блокировка разделов не по роли
app/api/**                          — все backend-роуты с проверкой прав на сервере
app/(dashboard)/**                   — экраны приложения
components/**                         — переиспользуемые UI-компоненты
```

## Что реализовано полностью

- 5 ролей с разграничением прав **и на фронте, и на backend** (см. `lib/rbac.ts` — каждый
  API-роут явно проверяет роль и возвращает 403, а не просто прячет кнопку)
- Филиалы: директор может создавать новые (раздел «Сотрудники»), фильтровать заявки,
  аналитику, отчёты по рекламе и кассу по конкретному филиалу или смотреть все сразу;
  региональный директор всегда видит только свой филиал; чат не ограничен по филиалам
- Канбан-доска заявок, 6 статусов в заданном порядке, drag-and-drop (`@hello-pangea/dnd`)
- Карточка клиента с 24-часовым окном правки имени/адреса/причины звонка для колл-центра
- Закрытие заявки: статус, сумма, автоматическое деление суммы (по умолчанию 50/50,
  процент регулируется), обязательное фото для «Готово», обязательная причина для «Отказ»
- Часовое окно правки уже закрытой заявки для директора/рег.директора
- Справочник источников рекламы, архивирование вместо удаления при использовании
- Ежедневные агрегированные отчёты по рекламе с фильтром по периоду и филиалу
- Аналитика рекламы и аналитика заявок с графиками и автоматическими выводами
- Касса: приход/расход/зарплаты, итог, график динамики, фильтр по филиалу
- Чат: личные сообщения и группы, вложения, системные уведомления мастеру
- Раздел «Сотрудники» и «Филиалы» (директор/рег.директор)
- Аудит-лог правок полей с временными ограничениями (`lib/audit.ts`)
- Тёмный сайдбар + светлая рабочая область, адаптивная вёрстка

## Упрощения / точки расширения

- **Email/push-уведомления** мастеру реализованы как in-app системное сообщение в чат
  (работает сразу) + заглушки `sendEmail`/`sendPush` в `lib/notify.ts` с TODO для
  подключения реального провайдера (Resend/SendGrid, Web Push/FCM)
- **Чат в реальном времени** — через поллинг раз в 4 секунды, а не WebSocket/SSE
- **Хранение файлов** — Vercel Blob (`app/api/upload/route.ts`). Локальная файловая система
  на serverless-хостингах только для чтения во время выполнения, поэтому файлы сохраняются
  в облако. Чтобы это работало (и локально, и в проде), один раз подключите в панели Vercel:
  **Storage → Create Database → Blob**, затем локально выполните `vercel env pull .env.local`,
  чтобы подтянуть токен `BLOB_READ_WRITE_TOKEN` — без него загрузка фото не заработает.

## Скрипты

```bash
npm run dev           # запуск в режиме разработки
npm run build          # прод-сборка
npm run db:generate     # prisma generate
npm run db:push           # создать/обновить таблицы в базе по текущей схеме
npm run db:seed             # заполнить демо-данными
npm run db:setup              # всё вместе (generate + push + seed)
```
