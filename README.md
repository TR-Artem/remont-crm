# РемонтCRM

Веб-приложение для управления заявками, рекламой и бухгалтерией службы бытового ремонта.
Стек: Next.js 15 (App Router) + TypeScript + Prisma + NextAuth v5 (Auth.js) + Tailwind CSS.

## ⚠️ Важно перед запуском

Этот проект был написан в изолированной среде без доступа к `binaries.prisma.sh`, поэтому
**`prisma generate` и `prisma migrate` ни разу не выполнялись и не проверялись здесь**.
Сам код написан внимательно и по документации, но перед использованием обязательно:

1. Выполните полную установку (см. ниже) на машине с обычным доступом в интернет.
2. Запустите `npm run dev` и пройдитесь по сценариям из раздела «Чек-лист» ТЗ.
3. Если увидите ошибки типов/рантайма — это ожидаемо для проекта такого объёма без
   раннего прогона; их можно поправить итеративно.

## Установка

```bash
npm install
npm run db:setup       # prisma generate + migrate + seed
npm run dev
```

Приложение поднимется на http://localhost:3000. Файл `.env` уже содержит
`DATABASE_URL="file:./dev.db"` и `AUTH_SECRET` для локальной разработки — для прода
замените оба значения.

### Демо-доступы (после `db:seed`, пароль везде `password123`)

| Логин        | Роль                    |
|--------------|-------------------------|
| director     | Директор                |
| regional     | Региональный директор   |
| admin        | Администратор           |
| callcenter   | Колл-центр              |
| master1      | Мастер                  |
| master2      | Мастер                  |

## База данных

По умолчанию используется **SQLite** (`prisma/schema.prisma`, `DATABASE_URL="file:./dev.db"`) —
самый быстрый способ поднять проект локально без установки PostgreSQL.

Для продакшена переключитесь на PostgreSQL:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

и укажите `DATABASE_URL="postgresql://user:pass@host:5432/remontcrm"`. Модель данных
совместима с обеими СУБД — статусы/роли/типы объявлены как `String` с валидацией на
уровне приложения (zod, см. `lib/validation.ts`), а не как нативные enum'ы, чтобы
миграция между SQLite (разработка) и PostgreSQL (прод) была безболезненной.

## Структура проекта

```
prisma/schema.prisma        — модель данных (раздел 9 ТЗ)
prisma/seed.ts               — демо-данные
lib/domain.ts                 — роли, статусы, права разделов — единый источник правды
lib/rbac.ts                    — серверная проверка прав (403), временные окна редактирования
lib/money.ts                    — деление суммы закрытия (раздел 2.4)
lib/auth.ts                      — конфигурация NextAuth v5
lib/notify.ts                     — уведомление мастера (in-app сразу, email/push — заглушки)
middleware.ts                      — редирект неавторизованных, блокировка разделов не по роли
app/api/**                          — все backend-роуты с проверкой прав на сервере
app/(dashboard)/**                   — экраны приложения
components/**                         — переиспользуемые UI-компоненты
```

## Что реализовано полностью

- 5 ролей с разграничением прав **и на фронте, и на backend** (см. `lib/rbac.ts` — каждый
  API-роут явно проверяет роль и возвращает 403, а не просто прячет кнопку)
- Канбан-доска заявок, 6 статусов в заданном порядке, drag-and-drop (`@hello-pangea/dnd`)
- Карточка клиента с 24-часовым окном правки имени/адреса/причины звонка для колл-центра
  (бейдж с обратным отсчётом, автоматическая блокировка полей по истечении)
- Закрытие заявки (админ/директор/рег.директор): статус, сумма, автоматическое деление
  суммы (по умолчанию 50/50, процент регулируется), обязательное фото для «Готово»,
  обязательная причина для «Отказ»
- Часовое окно правки уже закрытой заявки для директора/рег.директора, с автопересчётом
  связанных кассовых операций
- Справочник источников рекламы (фото, тип), архивирование вместо удаления при наличии
  использующих заявок
- Ежедневные агрегированные отчёты по рекламе с фильтром по периоду
- Аналитика рекламы: рейтинг источников, доли (столбчатая + круговая диаграммы),
  лучший/слабый источник, конверсия — считается автоматически из заявок
- Аналитика заявок: готово/отказ %, распределение по статусам, топ причин отказа
- Касса: приход (авто из закрытых заявок), расход (вручную), зарплаты мастеров (авто
  при закрытии + ручная корректировка), итог, таблица операций, график динамики по дням
- Чат: личные сообщения и группы, вложения (фото/видео), системные уведомления о новой
  заявке мастеру, экран «Доступ ограничен» при прямом переходе мастера в другой раздел
- Раздел «Сотрудники» (директор/рег.директор): создание пользователей с ролью и филиалом
- Аудит-лог правок полей с временными ограничениями (`lib/audit.ts`)
- Тёмный сайдбар + светлая рабочая область в заданной палитре, адаптивная вёрстка
  (сайдбар сворачивается в бургер-меню на мобильных)

## Упрощения / точки расширения (честно, чтобы не было сюрпризов)

- **Email/push-уведомления** мастеру о новой заявке реализованы как in-app системное
  сообщение в чат (работает сразу) + заглушки `sendEmail`/`sendPush` в `lib/notify.ts`
  с явным TODO для подключения реального провайдера (Resend/SendGrid, Web Push/FCM) —
  для реальной отправки нужны API-ключи, которых у меня нет.
- **Чат в реальном времени** реализован через поллинг раз в 4 секунды, а не WebSocket/SSE —
  проще в развёртывании, ТЗ допускает polling как вариант.
- **Хранение файлов** — локальная папка `/public/uploads` (см. `app/api/upload/route.ts`),
  как и допускает ТЗ. Для прода замените на S3-совместимое хранилище.
- Код **не был запущен** в среде разработки (см. предупреждение выше) — возможны мелкие
  опечатки/несостыковки типов, которые обычно всплывают за первые несколько минут `npm run dev`.

## Скрипты

```bash
npm run dev           # запуск в режиме разработки
npm run build          # прод-сборка
npm run db:generate     # prisma generate
npm run db:migrate       # применить миграции (создаёт dev.db)
npm run db:seed           # заполнить демо-данными
npm run db:setup           # всё вместе
```
